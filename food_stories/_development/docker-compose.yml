version: '3.1'

services:
  db:
    image: postgres
    ports: 
      - 5432:5432
    restart: always
    env_file: ../.env
    volumes:
      - ../db:/var/lib/postgresql/data

  redis:
    image: redis
    ports: 
      - 6379:6379
    restart: always